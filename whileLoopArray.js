const fruits = ["Mango", "Apple", "Guava"];
let i = 0;
while (i < fruits.length) {
  console.log(fruits[i]);
  i++;
}

// similar to the regular while loop and for loop for arrays, each and every element in the array can be accessed by using while loop
