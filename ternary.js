let age = 20;
// let drink;
// if (age > 5) {
//   console.log("tea");
// } else {
//   console.log("milk");
// }

let drink = age > 5 ? "tea" : "milk";
console.log(drink);

// instead of using if else we can use ternary operators in order to make the code simpler and shorter

// we need to store the true or false value in a variable first
// then we need to put the conditon and then use ? mark
// the value near the question mark print when the condition is true and the value after the semicolon is printed when the value is false
