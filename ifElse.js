// if else condition

let age = 18;
if (age < 20) {
  console.log("you are a noob");
} else {
  console.log("go watch pogo");
}

// in both the cases if checks the truthy values   ... it runs only when the conditions are true and when the conditons are false the else block searchForWorkspaceRoot... when all the conditions are true the code doesnt even care about the else block
let num = 11;
if (num % 2 === 0) {
  console.log("even");
} else {
  console.log("odd");
}
