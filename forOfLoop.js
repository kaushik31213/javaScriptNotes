const fruits = ["Mango", "Apple", "Guava"];
for (let fruit of fruits) {
  console.log(fruit);
}

// for of loop is a little different than the regular loops in arrays, it doesnt give the index but gives the value instead of it , that means it will print all the elements of the array according to its lengths
