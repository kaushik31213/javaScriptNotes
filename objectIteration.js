let person = {
  name: "Kaushik",
  age: 25,
  address: "Jagiroad",
  marks: [49, 40, 42, 45],
  "person color": "white",
};

// iterate means to use loops

// for in loop in object

// for (let key in person) {
//   // console.log(key);
//   // to print the key value pair of person
//   console.log(`${key} : ${person[key]}`);
// }

// it will print all the keys of the object

// console.log(Object.keys(person));
// // this will return an array of all the keys
// let val = Array.isArray(Object.keys(person));
// console.log(val);

// for of loop in objects

// for (let key of person) {
//   console.log(key);
// }

// we cannot use for of loop in objects
