const fruits = ["Mango", "Apple", "Guava"];
for (let fruit in fruits) {
  console.log(fruit);
}

// unliek for of loop which gives each element of an array, for in loop gives the index of each element in the array , just like the regular for loops

