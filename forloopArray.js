let names = ["Kaushik", "Kavery", "Kohli", "Rohit"];
let names2 = [];
for (let i = 0; i < names.length; i++) {
  names2.push(names[i].toUpperCase());
}
console.log(names.length);
console.log(names[1]);
console.log(names[names.length - 1]);
console.log(names2);

// for loop in array is similar to the regular for loops, the i in the for loop gives index and it can be used to print each and every value in the array
